# HestiaOS Community Edition — Aggregated Dependencies
# =====================================================
# Source: 01_hestiaos-ai01-stable/requirements.txt + mcp-server/requirements.txt
# Created: 2026-06-20
#
# Install: pip install -r requirements-community.txt
#
# Pin policy:
#   ~=  — compatible-release (patch updates allowed)
#   ==  — exact pin (security-sensitive packages)
#   >=  — minimum version (no upper bound)

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi~=0.135.2
uvicorn[standard]~=0.42.0
python-multipart~=0.0.9

# ── Data validation ────────────────────────────────────────────────────────
pydantic~=2.12.5

# ── HTTP clients ──────────────────────────────────────────────────────────
requests~=2.33.0
aiohttp~=3.13.3
httpx~=0.28.1
# aiofiles~=24.1.0 — downgraded to ~=23.2 for matrix-nio compatibility
aiofiles~=23.2

# ── Configuration / serialisation ─────────────────────────────────────────
pyyaml~=6.0

# ── System utilities ──────────────────────────────────────────────────────
psutil~=7.2.2
watchdog~=6.0.0

# ── AI / Memory ───────────────────────────────────────────────────────────
mem0ai==1.0.8
mcp>=1.0.0,<2.0.0

# ── Content processing ────────────────────────────────────────────────────
markdown~=3.0
beautifulsoup4~=4.14.3

# ── Logging / resilience ──────────────────────────────────────────────────
loguru~=0.7.3
tenacity~=9.1.4

# ── File locking ──────────────────────────────────────────────────────────
filelock~=3.29.0

# ── Matrix integration ────────────────────────────────────────────────────
matrix-nio~=0.24.0

# ── MCP Server (mcp-server) ────────────────────────────────────────────
numpy>=1.24.0
nomic>=3.0.0

# ── SDK (sdk) ──────────────────────────────────────────────────────────
# httpx and typer are declared in sdk/setup.py
